export interface Testimonial {
  id: string;
  name: string;
  role: string;
  location: string;
  quote: string;
  rating: number;
}

// <!-- PENDIENTE: testimonios reales de clientes de IP Sistemas -->
export const testimonialsData: Testimonial[] = [];
